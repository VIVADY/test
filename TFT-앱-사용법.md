# ♟️ TFT 도우미 — 나만의 롤토체스 앱

아이폰에서 **홈 화면 앱처럼** 쓰는 롤토체스 참고 앱입니다.
추천 덱 / 아이템 조합 / 증강·특성을 게임 중 홈 버튼(제스처)으로 잠깐 전환해서 봅니다.

> ⚠️ 아이폰(iOS)은 안드로이드와 달리 **다른 앱 위에 떠 있는 오버레이가 시스템적으로 불가능**합니다.
> 그래서 "게임 화면 위에 겹치기"가 아니라 **잠깐 전환해서 보는 컴패니언 앱** 방식입니다.
> (진짜 겹치는 오버레이를 원하면 아이패드/옛날폰에 이 앱을 띄워두고 옆에 두고 보세요.)

> 📱 **아이패드(가로)** 로 열면 탭 없이 **추천덱·아이템·증강이 한 화면에 3단**으로 펼쳐집니다.
> 아이폰(세로)에서는 탭 방식으로 자동 전환돼요. (같은 파일, 화면 크기에 따라 자동 조정)

---

## 1. 아이폰에 설치하기 (가장 쉬운 방법)

이 앱은 파일 하나(`index.html`)로 동작합니다. **인터넷에 올려서 주소로 접속** → **홈 화면에 추가**하면 끝.

### 방법 A — GitHub Pages로 무료 호스팅 (추천, 무료)
1. 이 저장소 → **Settings → Pages**
2. **Source**를 `Deploy from a branch`로 두고, 브랜치를 이 브랜치(또는 main)로 선택 후 저장
3. 잠시 뒤 생기는 주소 `https://<아이디>.github.io/<저장소>/` 로 아이폰 **Safari**에서 접속
4. 하단 **공유 버튼(⬆️)** → **"홈 화면에 추가"** → 완료!
   → 홈에 ♟️ 아이콘이 생기고, 탭하면 **주소창 없는 전체화면 앱**처럼 열립니다.

### 방법 B — 그냥 파일로 열기
`index.html`을 아이폰으로 보내서(에어드롭/파일앱) Safari로 열어도 되지만,
홈 화면 앱으로 만들려면 방법 A(웹 주소)가 필요합니다.

---

## 1-2. 덱 추천 기능 (내 재료 기반)

**「덱 추천」 탭**에서 지금 가진 **기본 아이템**을 탭하면, 그 재료로 만들기 유리한 덱을
**매칭률 순**으로 자동 정렬해 줍니다.

- 앱이 각 덱의 핵심 아이템이 어떤 기본템 2개로 조합되는지 알고 있어서,
  내 재료로 **핵심템을 몇 개나 만들 수 있는지** 계산해 추천합니다.
- 각 덱마다 `✓ 만들 수 있음 / ◑ 재료 1개 더 필요 / · 없음` 으로 표시돼요.
- 받은 **증강**을 함께 선택하면 그 증강에 맞는 덱에 가산점이 붙습니다.
- 아이폰은 게임 화면을 자동으로 읽어올 수 없어서(오버레이·화면인식 불가),
  **재료를 직접 탭하는 입력 방식**으로 만들었습니다.

## 2. 내용 업데이트하기 (패치마다)

코딩 몰라도 됩니다. `index.html` 파일에서 **`★★★ DATA`** 라고 적힌 부분만 찾아
**따옴표 안의 글자**를 바꾸면 됩니다.

- **추천 덱**: `DECKS` 목록 — 덱 이름, 티어(S/A/B/C), 캐리 챔피언, 아이템, 조합, 증강
- **아이템 조합**: `COMPONENTS`(기본템), `COMBOS`(재료 + 재료 → 완성템)
- **증강**: `AUGMENTS` — 이름, 등급(prismatic/gold/silver), 설명
- **특성**: `TRAITS` — 이름, 활성 구간, 설명

예) 덱 하나 추가:
```js
{
  name: "새 덱 이름",
  tier: "S", level: "8 레벨링", playstyle: "운영 방식",
  carries: [ { unit: "캐리 챔피언", items: ["아이템1","아이템2","아이템3"] } ],
  comp: ["유닛1","유닛2","유닛3"],
  augments: ["증강1"],
  notes: "운영 팁",
},
```

---

## 3. 다음 단계 (원하면 확장 가능)

지금 데이터는 **직접 입력하는 예시**입니다. 나중에 이런 것도 붙일 수 있어요:

- **실시간 데이터**: Riot 공식 API(TFT 매치/랭크) 연동 → 내 전적·현재 판 분석
- **메타 자동 갱신**: 메타 사이트 데이터로 티어표 자동 업데이트 (해당 사이트 이용약관 확인 필요)
- **PiP 편법 오버레이**: 기술 난이도 높음. 개인용(사이드로드)이라면 시도 가능

필요하면 여기서부터 하나씩 같이 붙여드릴게요.
